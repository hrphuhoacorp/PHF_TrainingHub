PHF HR ROUTE 1.33.8 - COPY ĐÈ

Copy đè đúng 2 file vào thư mục project đang chạy:
1. index.html
2. assets/js/phf-progress-profile.js

Không chạy SQL.
Sau khi copy: dừng npm start, chạy lại, Ctrl+F5 và mở /admin/noi-dung.

Nguyên nhân sửa:
Resume legacy trong phf-progress-profile.js chạy setTimeout(restore, 0) trên mọi URL,
làm màn đã lưu ghi đè deep link vừa được Router render. Bản này chỉ cho Resume chạy
ở /, /login, /admin, /ql, /hv; mọi deep link thuộc quyền Router trung tâm.
