{
  "version": "1.53.2",
  "fingerprint": "1.53.2-knl-permissions-picker-numbering-fix-LOCAL-ONLY",
  "builtAt": "2026-08-13T10:40:00+07:00",
  "notes": "1.53.2 [LOCAL ONLY, CHƯA PUSH/DEPLOY]: PHF HR/KNL — hoàn thiện màn 'Phân quyền KNL' trước khi sang Dashboard Gate 2. Không đụng Dashboard/Hồ sơ thu nhập chi tiết/backend/permission contract/data — chỉ phf-knl-app.js + phf-knl.css. (1) Cột 'Chọn nhân sự': thêm 3 filter cascading/facet Phòng ban/Chi nhánh/Chức danh (accountFacetOptions/accountMatchesFilters, cùng kỹ thuật knlEmployeePickerHtml của batch c22dced — reuse LOGIC facet, không copy component vì khác model chọn-1 vs multi-select/navigate), option mỗi filter tự tính lại theo các filter+search khác đang áp dụng, tự bỏ chọn nếu không còn hợp lệ (normalizeAccountFilters). Đổi sang partial refresh (refreshAccountResults/accountResultsHtml, tách khỏi input search) để KHÔNG mất focus ô tìm kiếm giữa lúc gõ — trước đây mỗi keystroke render lại toàn bộ panel. selectAccount()/loadPermissions()/saveGrant() KHÔNG đổi. (2) Numbering: '4. Phạm vi Thu nhập' trước đây gắn với accordion mở/đóng (incomeConfigOpen) khiến '5'/'6' nhảy số khi collapse — sửa: số 4 gắn với income_view ĐANG BẬT (không phụ thuộc mở/đóng), Lý do/Nâng cao tính động thành 4/5 (không có Phạm vi Thu nhập) hoặc 5/6 (có) — render đúng theo state thực, không fake bằng CSS. (3) BUG THẬT phát hiện qua trace (không phải giả thuyết): role-card click handler ghi đè vô điều kiện g.peopleScope mỗi khi bấm — kể cả bấm lại đúng role card ĐANG active. Tài khoản Trợ lý Giám đốc có peopleScope.type='department' với NHIỀU phòng ban (cấu hình thật, hợp lệ theo scope()/KNL_SCOPE_TYPES ở lib/knl-permissions.js — không phải lỗi dữ liệu) sẽ bị âm thầm reset về {type:'all_company'} nếu Admin chỉ vào sửa Xem Thu nhập/Lý do mà lỡ chạm lại role card. Fix: chỉ áp peopleScope mặc định theo vai trò khi vai trò THỰC SỰ đổi (roleChanged = roleKeyFromPreset(g.presetCode)!==roleDef.key trước khi mutate) — nếu Admin chuyển hẳn sang vai trò khác rồi quay lại 'Trợ lý Giám đốc' thì vẫn reset về all_company như cũ (đúng ý định preset gốc cho 1 lần gán mới), chỉ khác là click lặp lại role đang active không còn phá dữ liệu. incomeScope xác nhận độc lập hoàn toàn với peopleScope ở mọi lớp (normalizeGrant/normalizeIncomeScope/scope() không đọc chéo nhau) — không đổi gì ở đây, chỉ xác nhận qua trace. KHÔNG có cơ chế reserve/restore cho peopleScope.type='department' giống reservedEmployees của type='employees' (đó sẽ là thay đổi backend, ngoài phạm vi batch này — nếu cần, phải trace/duyệt riêng). Regression: permissions-scope (29 case, backend contract không đổi) + initial-permission-seed ALL PASS, npm run check PASS. File sửa: assets/js/knl/phf-knl-app.js, assets/css/phf-knl.css, index.html, build-info.json. RELEASE GATE: LOCAL ONLY — KHÔNG git push, KHÔNG deploy Vercel.",
  "build": "1.53.2"
}
