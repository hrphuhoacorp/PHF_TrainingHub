{
  "version": "1.50.14",
  "fingerprint": "1.50.14-knl-grade-matrix-save-feedback-p0",
  "builtAt": "2026-08-11T09:00:00+07:00",
  "notes": "1.50.14: P0 — KNL Tiêu chuẩn bậc 'Lưu ma trận' had zero click feedback (no loading state, no success confirmation, error paragraph only rendered at the very bottom of a long grid). Traced end-to-end with a real jsdom click-through against the actual shipped file (scripts/test-knl-grade-matrix-save-ui-1.50.13.js, new jsdom devDependency): the handler, payload and saveKnlGradeMatrix RPC call were all already correct after 1.50.13 for all 3 required cases (edit M-level on a saved matrix, add/delete a bậc on a saved matrix, first save of an empty prefilled matrix) — the P0 'click does nothing' report was a real UX gap (successful and failed saves looked identical to a dead click), not a broken wire. Added foundationState.gradeSaving/gradeMessage: button shows 'Đang lưu…' and disables (plus the version dropdown and all add/remove/cell controls) while the RPC is in flight, a visible status line appears right next to the button for both success ('Đã lưu ma trận thành công.') and error (RPC message shown verbatim, button re-enables for retry) — no silent fail. Did not touch Production data; did not regress the 1.50.13 inactive-framework filter or saved-state add/delete grade fix (same DOM test suite asserts both).",
  "build": "1.50.14"
}
