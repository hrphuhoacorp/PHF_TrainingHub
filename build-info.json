{
  "version": "1.51.4",
  "fingerprint": "1.51.4-knl-income-assignment-n1-retroactive-preview",
  "builtAt": "2026-08-11T20:10:00+07:00",
  "notes": "1.51.4 KNL Thu nhập Batch 2 (Admin gán cơ cấu thu nhập theo kỳ): assignFormHtml/bindAssignForm (assets/js/knl/phf-knl-app.js) — kỳ lương áp dụng mặc định gợi ý N+1 (tháng hiện tại+1); chọn Bậc tự tick sẵn các khoản chuẩn (PC nghiệp vụ/PC quản lý/Tiền cơm), Admin bỏ tick khoản không áp dụng; thêm preview functional (không polish UI) so kỳ trước/kỳ mới + cảnh báo hồi tố + cảnh báo ghi đè khi trùng kỳ hiện tại; mức thử việc gợi ý 6.800.000 (điều chỉnh được); chuyển thử việc sang chính thức tự gợi ý Bậc 1 của Version vừa chọn, Admin vẫn phải tự xác nhận trước khi lưu. saveKnlEmployeeIncome (lib/knl-foundation.js) — enforce server-side: kỳ trước kỳ khuyến nghị N+1 bắt buộc reason >=5 ký tự (KNL_RETROACTIVE_REASON_REQUIRED), không đổi RPC payload/atomicity. Thêm listKnlEmployeeCompensationPeriods (đọc toàn bộ kỳ của 1 nhân sự trực tiếp từ knl_employee_compensation_assignments, không phải diff kỹ thuật) - cùng permission income_view/incomeScope hiện hành, không mở quyền mới - phục vụ khối 'Lịch sử thu nhập theo kỳ' tương lai, chưa xây UI mới. Wired vào server.js (dispatcher /api/data thật đang chạy trên Render) và api/data.js (không dùng trên deploy hiện tại, giữ đồng bộ). Không migration: knl_save_employee_compensation RPC đã atomic (assignment+history 1 transaction, đã đọc lại SQL xác nhận). Không có field canonical ngày bắt đầu thử việc trong schema compensation -> KHÔNG tự implement cảnh báo hết-thử-việc theo ngày (tránh invent field mới); gợi ý Bậc 1 chỉ kích hoạt khi Admin chủ động chuyển loại hình trên form. Không đụng Proposal/Checklist/Classroom/Training Hub/legacy employee_compensation/permission policy. Regression: scripts/test-knl-income-batch2-2026-08.js (mới, 11 checks) + batch1 (19) + test-knl-permissions-scope.js (29) + npm run check — ALL PASS.",
  "build": "1.51.4"
}
