{
  "version": "1.52.16",
  "fingerprint": "1.52.16-knl-grade-proposal-tab-loader-fix-LOCAL-ONLY",
  "builtAt": "2026-08-12T19:10:00+07:00",
  "notes": "1.52.16 [LOCAL ONLY, CHƯA PUSH/DEPLOY]: fix initial-load/tab-state bug ở 'Đề xuất nâng bậc' — CHỈ frontend lifecycle, KHÔNG đụng backend/workflow/permission. TRACE xác nhận: listMyGradePromotionProposals() (lib/knl-grade-proposals.js) đã ĐÚNG từ trước (or subject_employee_code.eq/created_by.eq — verify trực tiếp bằng session thật PHF012: trả đúng 2 proposal bao gồm PHF082) — bug 100% ở frontend. Root cause thật: bindGpNav() (assets/js/knl/phf-knl-app.js) khi bấm tab CHỈ đổi gpState.view rồi renderGpBody() ngay — KHÔNG gọi loader nào (loadGpMine/loadGpAwaiting/loadGpVisible) — nên tab vừa bấm render từ gpState.mine/awaiting/visible vẫn là [] (chưa fetch bao giờ trong phiên đó), hiện đúng y hệt UI 'không có dữ liệu' dù data thật tồn tại. renderGradePromotionSection() (chạy khi mount/route-change qua phfRenderKnl) là NƠI DUY NHẤT trước đây gọi đúng loader theo gpState.view — nên chỉ có 'chuyển sang màn KNL khác rồi quay lại' (đi qua router, remount) mới vô tình fetch đúng, đúng y hệt hiện tượng PHF report. Fix: tách logic dispatch loader ra hàm dùng chung gpLoadActiveView(root) (đọc gpState.lastCapabilities/lastIsAdmin đã lưu sẵn từ lần mount), gọi từ CẢ renderGradePromotionSection() (mount) LẪN bindGpNav() (mỗi lần bấm tab) — sửa 1 chỗ lifecycle chung, không vá riêng theo role. Thêm loading/loaded/loadError RIÊNG cho từng dataset (mineLoading/mineLoadError, awaitingLoading/awaitingLoadError, visibleLoading/visibleLoadError) — gpListBodyHtml() qua gpDatasetBodyHtml() helper mới giờ phân biệt đúng loading (spinner text) > loadError (hiện lỗi thật, KHÔNG giả thành rỗng) > loaded-empty thật (message rỗng cũ, chỉ hiện khi fetch xong và list rỗng thật) — tách biệt hoàn toàn khỏi gpState.error/message (giữ nguyên nghĩa cũ: thông báo sau agree/reject/withdraw/tạo đề xuất). Race-safety khi chuyển tab nhanh: KHÔNG cần token/request-id — mỗi loader chỉ ghi vào state field CỦA CHÍNH DATASET ĐÓ (mine/awaiting/visible tách biệt), và render luôn đọc gpState.view HIỆN TẠI tại thời điểm render nên response trễ của tab cũ không bao giờ đè nhầm dataset của tab đang active. Duplicate-request guard: mỗi loader có early-return nếu chính nó đang loading (chặn double-click cùng tab), KHÔNG cache 'loaded rồi thì thôi' — mỗi lần chuyển tab luôn refetch để đảm bảo correctness (đúng yêu cầu 'correctness quan trọng hơn optimization'). Verify tay case PHF082 thật qua HTTP thật (cookie ký hợp lệ cho PHF012, KHÔNG mutate DB): listMyKnlGradePromotionProposals trả đúng 2 proposal gồm PHF082 — xác nhận trace backend đúng, bug hoàn toàn ở tầng UI lifecycle như đã chỉ ra. node --check assets/js/knl/phf-knl-app.js OK. Regression: toàn bộ test-knl-grade-proposals.js (68 case) + test-knl-permissions-scope.js (29 case) + approver-options + income batch1/2 + people-income-gate — ALL PASS (không đổi vì backend không đổi). Không migration, không đổi peopleScope/incomeScope/permission model/approval status machine/routingSnapshot/current_step_index/Checklist/Classroom/Training Hub. Không redesign/polish UI, không fork UI theo role. RELEASE GATE: LOCAL ONLY — build-info + ?v= bump để npm run check PASS local, KHÔNG git push, KHÔNG deploy Vercel.",
  "build": "1.52.16"
}
