{
  "version": "1.52.18",
  "fingerprint": "1.52.18-knl-grade-proposal-visual-cleanup-LOCAL-ONLY",
  "builtAt": "2026-08-12T21:05:00+07:00",
  "notes": "1.52.18 [LOCAL ONLY, CHƯA PUSH/DEPLOY]: final visual/presentation cleanup cho 'Đề xuất nâng bậc' — KHÔNG mở nghiệp vụ mới, KHÔNG đổi workflow/permission/approval chain. TRACE identity presentation xác nhận root cause: attachListPresentation()/getGradePromotionProposalDetail() (lib/knl-grade-proposals.js) trước đây lấy createdByEmployeeCode/actorEmployeeCode từ knl_grade_promotion_proposal_steps.actor_employee_code — cột này lưu ĐÚNG tại thời điểm ghi audit, nhưng proposal PHF082 được TẠO TRƯỚC identity fix (9bc4bae) nên step propose của nó mang giá trị actor_employee_code cũ ('HV-0934510194', internal employeeId của Training Hub) — hiện thẳng ra UI đúng như PHF report, KHÔNG phải bug mới, là hệ quả dữ liệu lịch sử. Fix: thêm getEmployeeCodesByAccountIds(ids) (lib/auth.js, EXPORT mới, THUẦN READ user_accounts.employee_code theo account id — auth.js vẫn là module DUY NHẤT được phép đọc user_accounts, giữ đúng ranh giới kiến trúc hiện có) — resolve mã CHUẨN LUÔN LUÔN HIỆN HÀNH của account (created_by trên proposal, actor_id trên từng step) thay vì dùng giá trị đã persist tại thời điểm ghi audit. attachListPresentation() đổi từ query steps sang query user_accounts theo created_by (bulk, 1 query). getGradePromotionProposalDetail() resolve thêm proposal.createdByEmployeeCode + publicStep() nhận tham số resolvedEmployeeCode (ưu tiên tuyệt đối, không fallback về giá trị cũ nếu không resolve được — đúng rule 'không hiện internal ID nếu không có mã chuẩn'). RAW audit (actor_employee_code trong DB) giữ nguyên 100%, không mutate — chỉ đổi field trả về API. Verify tay qua HTTP thật (read-only): list + detail proposal PHF082 giờ trả đúng 'PHF012' thay vì 'HV-0934510194', raw DB step vẫn còn 'HV-0934510194' (audit nguyên vẹn). Frontend (assets/js/knl/phf-knl-app.js): bỏ heuristic regex gpLooksLikeStandardEmployeeCode() ở Lịch sử xử lý (không cần nữa vì backend đã resolve sạch, tránh duplicate logic — đúng chỉ đạo 'tái dùng source identity chuẩn đã có'), thêm createdByEmployeeCode vào meta 'Người đề xuất' ở detail header. Layout: .phfk-gp-detail đổi max-width 720px (lệch trái, dư khoảng trắng phải — đúng PHF report) sang max-width:1200px + margin:0 auto — mọi panel (Thông tin đề xuất/Tiến trình xử lý/Lịch sử xử lý/Quyết định của bạn/Rút đề xuất) tự động cùng width/cùng trục vì đều là block con trực tiếp của .phfk-gp-detail, không chia 2 cột. Typography: .phfk-gp-detail-meta dt (10px, letter-spacing .05em -> 12px, .01em, weight 700 — bớt 'kiểu poster'), .phfk-pill (10.5px -> 11px), .phfk-gp-wf-node small (10.5px -> 11.5px), .phfk-gp-chain-node/i (10.5/8.8px -> 11/9.5px) — theo đúng scale 11.5-12.5px cho metadata/11-12px cho badge đã cho, tất cả scoped .phfk-gp-*, KHÔNG đổi .phfk-panel/.phfk-table/.phfk-domain-tabs dùng sitewide. Test fixture: scripts/test-knl-grade-proposals.js mock Supabase thêm bảng user_accounts (STATE.accounts, seed qua grant() có sẵn) để hỗ trợ getEmployeeCodesByAccountIds() trong môi trường test in-memory — không đổi assertion nào, chỉ thêm khả năng mock. Regression: test-knl-grade-proposals.js (68 case) + test-knl-permissions-scope.js (29 case) + approver-options + income batch1/2 + people-income-gate + employee-profile + test-auth-accounts-consolidation (13 case) — ALL PASS. node --check tất cả file sửa OK, CSS brace-balance OK (683/683). Không migration, không mutate proposal/step data, không đổi routingSnapshot/current_step_index/status machine/agree_proposal/approve_proposal/peopleScope/incomeScope/compensation/KNL assignment/Checklist/Classroom/Training Hub, không sửa residual knl-surveys.js. RELEASE GATE: LOCAL ONLY — build-info + ?v= bump để npm run check PASS local, KHÔNG git push, KHÔNG deploy Vercel.",
  "build": "1.52.18"
}
