{
  "version": "1.53.5",
  "fingerprint": "1.53.5-knl-permissions-dept-multiselect-LOCAL-ONLY",
  "builtAt": "2026-08-13T13:05:00+07:00",
  "notes": "1.53.5 [LOCAL ONLY, CHƯA PUSH/DEPLOY]: PHF HR/KNL — thay control 'Phòng ban (ngăn cách bằng dấu phẩy)' trong Thiết lập nâng cao (peopleScope, Phạm vi nhân sự) bằng multi-select thật từ Organization Master, bảo toàn tuyệt đối quyền hiện tại của Trợ lý Tiên. KHÔNG đụng Dashboard, KHÔNG đổi permission model/backend/data contract, KHÔNG hard-code employee_code/tên Tiên, KHÔNG reseed/mutate DB. Trace trước khi code xác nhận: peopleScope.type='department' với NHIỀU values đã là combination hợp lệ/được backend hỗ trợ sẵn (lib/knl-permissions.js scope(): normalizeScopeList trả về mảng string, không giới hạn 1 giá trị) — control cũ chỉ là vấn đề PRESENTATION (text input tự split theo dấu phẩy thành mảng), KHÔNG phải giới hạn dữ liệu; incomeScope xác nhận độc lập hoàn toàn với peopleScope ở normalizeGrant()/normalizeIncomeScope()/scope() (không đọc chéo field nào) — không đổi gì incomeScope. Đổi UI: advancedSectionHtml() bỏ hẳn input text 'ngăn cách bằng dấu phẩy' cho cả 2 loại department/employees; thêm advScopeDepartmentPickerHtml() (checkbox multi-select, danh mục department lấy từ permState.incomeValues — CHUNG roster listKnlPeople đã tải cho Phạm vi Thu nhập, cùng 1 nguồn Organization Master thật, chỉ khác selected values đọc g.peopleScope thay vì g.capabilities.incomeScope) và advScopeEmployeePickerHtml() (tái dùng employeePickerFiltersHtml/employeePickerListHtml đã có sẵn cho TBP/income-employee picker, prefix 'knl-adv-emp', permState.advEmployees mới). Stored contract KHÔNG đổi — peopleScope.values vẫn là mảng string y hệt trước, multi-select chỉ đổi CÁCH NHẬP nên round-trip an toàn tuyệt đối (verify qua permissions-scope test không đổi). Preload: selectAccount() và saveGrant() giờ tự loadIncomeValues()/loadAdvEmployees() khi peopleScope.type đang lưu là department/employees, để checkbox hiện đúng trạng thái đã chọn ngay khi mở tài khoản (không cần Admin tự mở Thiết lập nâng cao trước) — đây chính là điểm bảo vệ cấu hình thật của Trợ lý Tiên. Fix bug phụ phát hiện khi rewrite: advScopeType change handler trước đây giữ nguyên g.peopleScope.values cũ khi đổi loại phạm vi (vd đổi từ 'nhân sự cụ thể' sang 'phòng ban' vẫn mang theo mã nhân sự cũ lẫn vào mảng values) — nay reset values về [] khi type thực sự đổi, cùng nguyên tắc đã áp dụng sẵn cho incomeScope-type. Regression: permissions-scope (29 case, backend contract không đổi) + initial-permission-seed ALL PASS, npm run check PASS. File sửa: assets/js/knl/phf-knl-app.js, index.html, build-info.json (không đụng CSS — toàn bộ picker mới tái dùng class đã có). RELEASE GATE: LOCAL ONLY — KHÔNG git push, KHÔNG deploy Vercel.",
  "build": "1.53.5"
}
