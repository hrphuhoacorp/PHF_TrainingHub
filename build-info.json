{
  "version": "1.52.17",
  "fingerprint": "1.52.17-knl-grade-proposal-detail-polish-LOCAL-ONLY",
  "builtAt": "2026-08-12T20:15:00+07:00",
  "notes": "1.52.17 [LOCAL ONLY, CHƯA PUSH/DEPLOY]: hoàn thiện UI/UX + state refresh cho 'Đề xuất nâng bậc' — CHỈ presentation/frontend lifecycle, KHÔNG mở workflow/business rule mới, KHÔNG đụng lib/*.js. 4 cụm: (1) Polish detail — gpDetailHtml() viết lại hierarchy: header compact (pill trạng thái + tên·mã NV cỡ 19px, không phình to + bậc hiện tại→đề xuất + dl meta Người đề xuất/Ngày tạo + lý do), Tiến trình xử lý (gpWorkflowOverviewHtml, giữ nguyên logic mục batch trước) đứng TRƯỚC, Lịch sử xử lý đổi tên từ 'Timeline', bọc <details> collapsible (mặc định mở nếu <=3 bước, đóng nếu dài), ẨN employeeCode dạng legacy/internal (không khớp regex mã PHF chuẩn /^[A-Z]{2,8}[0-9]{2,6}$/) khỏi hiển thị công khai — raw giữ ở thuộc tính title (dev-safe) — KHÔNG map/đoán, chỉ ẩn nếu không giống mã chuẩn (gpLooksLikeStandardEmployeeCode() mới). 'Xử lý' đổi thành 'Quyết định của bạn' — Đồng ý luôn hiện sẵn (đường chính), Không đồng ý dùng <details><summary> để lý do CHỈ mở khi bấm (progressive disclosure), KHÔNG đổi validate backend. Rút đề xuất tách khỏi hierarchy chính, đặt cuối detail dạng <details> nhỏ (không .phfk-panel to), không cùng trọng lượng thị giác với workflow. (2) Refetch sau action — nút '← Quay lại' đổi từ renderGpBody() sang gpLoadActiveView() (tái dùng ĐÚNG helper đã tạo ở fix tab-loading batch trước, KHÔNG kiến trúc state mới) — quay lại list sau agree/reject/withdraw luôn refetch đúng dataset của tab đang active, list KHÔNG còn giữ proposal cũ. Double-submit guard: 1 flag gpState.detailActionBusy dùng chung cho cả 3 form agree/reject/withdraw (early-return nếu đang busy, disable input/nút khi busy=true, đọc lại mỗi lần render — cùng pattern c.submitting đã có ở form Tạo đề xuất) — FormData đọc TRƯỚC khi set busy+render để không mất data khi form bị innerHTML-replace. Lỗi action vẫn KHÔNG mutate state như thành công (giữ nguyên try/catch cũ, chỉ thêm busy flag). (3) Admin monitoring — đã đúng từ batch trước (listVisibleGradePromotionProposals/gpChainCompactHtml/gpWaitingCellHtml), batch này chỉ verify lại + polish typography scoped .phfk-gp-table (không đụng .phfk-table dùng chung toàn KNL). (4) Polish CSS — toàn bộ class mới đều scoped .phfk-gp-* (detail-head/detail-title/detail-grade/detail-meta/history/decision/withdraw), KHÔNG đổi .phfk-panel/.phfk-table/.phfk-domain-tabs/.phfk-empty/.phfk-loading dùng chung sitewide (tránh ảnh hưởng Nhân sự/Compensation/Framework/Permissions/Survey ngoài phạm vi batch). Mobile: bổ sung padding-bottom + full-width CTA cho khu quyết định/rút đề xuất dưới 760px, cộng thêm vào body.phf-knl-mode{padding-bottom} sitewide đã có sẵn (không thay rule global, chỉ thêm an toàn cho riêng module này). node --check OK, CSS brace-balance OK (683/683). Regression: test-knl-grade-proposals.js (68 case) + test-knl-permissions-scope.js (29 case) + approver-options + income batch1/2 + people-income-gate — ALL PASS (backend không đổi). Không migration, không đổi routingSnapshot/current_step_index/status machine/agree_proposal/approve_proposal/peopleScope/incomeScope/compensation/KNL assignment/Checklist/Classroom/Training Hub, không sửa residual knl-surveys.js. RELEASE GATE: LOCAL ONLY — build-info + ?v= bump để npm run check PASS local, KHÔNG git push, KHÔNG deploy Vercel.",
  "build": "1.52.17"
}
