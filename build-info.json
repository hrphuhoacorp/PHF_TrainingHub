{
  "version": "1.52.12",
  "fingerprint": "1.52.12-knl-people-canviewincome-per-row-LOCAL-ONLY",
  "builtAt": "2026-08-12T15:05:00+07:00",
  "notes": "1.52.12 [LOCAL ONLY, CHƯA PUSH/DEPLOY]: fix bug UX/rule đã trace ở batch trước — nút 'Xem thu nhập' trên /ql/knl/nhan-su (và /admin, /hv dùng chung render) trước đây gate bằng cờ phẳng cấp-trang peopleCanViewIncome=isAdmin||capabilities.income_view===true, áp cho MỌI row bất kể employee đó có thật sự nằm trong incomeScope hay không — khi peopleScope rộng hơn incomeScope, nút hiện sai cho nhân sự ngoài incomeScope (backend vẫn chặn đúng khi click, KHÔNG leak data, nhưng sai rule đã chốt). Fix: (1) Di dời incomeScopeAllows từ lib/knl-foundation.js sang lib/knl-permissions.js (module trung lập, không phụ thuộc knl-people.js/knl-foundation.js) để tránh circular require — lib/knl-foundation.js require lại và re-export nguyên vẹn (public API cũ incomeScopeAllows từ lib/knl-foundation không đổi, verify bằng node -e so sánh reference identity + toàn bộ test cũ dùng require('../lib/knl-foundation').incomeScopeAllows vẫn PASS nguyên). (2) lib/knl-people.js:listKnlPeople bổ sung field canViewIncome per-row, tính bằng ĐÚNG incomeScopeAllows (cùng hàm dùng thật ở getKnlEmployeeIncome) — không suy diễn riêng, không merge peopleScope+incomeScope, chỉ chấm độc lập trên từng row đã lọc theo peopleScope như cũ; response CHỈ bổ sung đúng 1 field mới, không đổi field nào khác. (3) assets/js/knl/phf-knl-app.js:peopleTable() — cột 'Thu nhập' vẫn dùng cờ phẳng peopleCanViewIncome để quyết định CÓ hiện cột hay không (giữ cấu trúc layout hiện hữu), nhưng nút bên trong TỪNG row giờ theo đúng p.canViewIncome===true; false -> ô rỗng, không placeholder giả, không đổi layout. Backend enforcement (getKnlEmployeeIncome/incomeScopeAllows) không đổi 1 dòng nào — chỉ frontend gate + 1 field mới ở listKnlPeople. Test: scripts/test-knl-people-income-gate-2026-08.js (mới, 15 assertion: Admin toàn bộ true, Tiên fixture đúng, synthetic split-scope xác nhận PHF036 trong peopleScope nhưng ngoài incomeScope -> canViewIncome=false trong khi vẫn xuất hiện trong list, PHF034 ngoài peopleScope không có trong list, user không có income_view -> canViewIncome=false toàn bộ, gọi API trực tiếp ngoài incomeScope vẫn 403, response fields cũ nguyên vẹn) — ALL PASS; frontend peopleTable() render test riêng (button chỉ render đúng row canViewIncome=true, không placeholder, cột ẩn hoàn toàn khi viewer không có income_view) — ALL PASS; regression toàn bộ test permission/income hiện có (batch1/batch2/next-compensation-grade/permissions-scope/initial-permission-seed/framework-batch1/batch2/survey-v1) — ALL PASS, không có test nào fail do di dời incomeScopeAllows. Không migration, không đổi peopleScope/incomeScope/preset/permission model, không đổi Salary/Income business logic ngoài việc thêm field đọc, không đổi Proposal/KNL assignment/Checklist/Classroom/Training Hub. RELEASE GATE: LOCAL ONLY — build-info bump để npm run check PASS local, KHÔNG git push, KHÔNG deploy Vercel.",
  "build": "1.52.12"
}
