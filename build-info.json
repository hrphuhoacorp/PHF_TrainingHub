{
  "version": "1.52.15",
  "fingerprint": "1.52.15-knl-grade-proposal-admin-monitoring-LOCAL-ONLY",
  "builtAt": "2026-08-12T18:20:00+07:00",
  "notes": "1.52.15 [LOCAL ONLY, CHƯA PUSH/DEPLOY]: Admin monitoring cho 'Đề xuất nâng bậc' — tab 'Danh sách' hiển thị đủ workflow toàn công ty, KHÔNG đổi approval chain/current-step business rule (đã checkpoint identity fix 9bc4bae). TRACE trước khi code xác nhận: listVisibleGradePromotionProposals() (lib/knl-grade-proposals.js) đã cho Admin (admin_recovery) READ toàn bộ proposal công ty từ trước — KHÔNG phải thay đổi permission ở batch này, chỉ thiếu field presentation. Backend: thêm attachListPresentation() — bulk-đọc employee_code người tạo từ knl_grade_promotion_proposal_steps (step_index 0, action 'propose'; created_by trên proposal là account id chứ không phải employee_code) + title subject/creator từ Organization Master (loadKnlOrganizationRows), gắn createdByEmployeeCode/subjectTitle/createdByTitle THUẦN presentation vào response của listVisibleGradePromotionProposals — không đổi field nào publicProposal() đã có, không đổi scope filter non-admin, không đổi permission/business rule. getGradePromotionProposalDetail() (liveChain/isMyTurn) và processGradePromotionProposalStep() (action gating) giữ nguyên 100% — Admin chỉ thấy nút Duyệt khi liveChain[currentStepIndex].tier==='final', đúng như trước. Frontend (assets/js/knl/phf-knl-app.js): thêm gpStepStatus()/gpShortName()/gpWaitingStep()/gpChainCompactHtml()/gpWaitingCellHtml() — derive 100% từ routing_snapshot/current_step_index/status đã persist (KHÔNG re-resolve live chain cho list vì tốn/không cần, KHÔNG invent state, KHÔNG hardcode tên/số tầng). gpRow()/gpTable() viết lại: cột Nhân sự/Bậc/Người đề xuất/Tiến trình/Đang chờ/Trạng thái/Thao tác — dùng CHUNG cho cả 3 tab (Cần tôi xử lý/Đề xuất của tôi/Danh sách), khác biệt chỉ ở data/permission backend trả, không fork UI. gpDetailHtml() thêm block 'Tiến trình xử lý' (gpWorkflowOverviewHtml, mới) render từ d.liveChain (live re-resolve, CÙNG nguồn isMyTurn) + d.proposal.currentStepIndex + d.steps (actor thật cho từng vị trí done, tránh sai tên khi có reassign) — tách biệt hoàn toàn với Timeline audit hiện có (giữ nguyên). CSS (assets/css/phf-knl.css): .phfk-pill (trước đây không style, giờ pending/approved/rejected/withdrawn), .phfk-gp-chain* (compact chain trong bảng), .phfk-gp-wf-* (workflow overview dọc trong detail) — theo đúng ngôn ngữ thiết kế hiện có (viền mỏng, cam nhẹ cho current, đỏ chỉ cho reject). Verify tay case PHF082 thật (read-only, KHÔNG mutate): current_step_index=0 vẫn nguyên, gpWaitingStep/gpChainCompactHtml/gpWorkflowOverviewHtml trace tay khớp đúng kỳ vọng (Thắng 'Đang chờ' → PHF004 → Admin, isMyTurn=false vì tier agree chưa phải final). createdByEmployeeCode của proposal PHF082 trả 'HV-0934510194' (residual, không sửa) vì step_index 0 của proposal này được ghi TRƯỚC identity fix 9bc4bae — dữ liệu lịch sử thật, KHÔNG mutate để 'đẹp' demo, proposal tạo mới sau fix sẽ đúng PHF-code. Test: toàn bộ scripts/test-knl-grade-proposals.js (68 case) + test-knl-permissions-scope.js (29 case) + approver-options + employee-profile + income batch1/2 + people-income-gate + competency service/standard/assignment-design + batch2 + survey-v1 + initial-permission-seed — ALL PASS. node --check assets/js/knl/phf-knl-app.js OK. Không migration, không đổi peopleScope/incomeScope/permission model/approval status machine/compensation/KNL assignment/Checklist/Classroom/Training Hub. Không đụng tab 'Tạo đề xuất' (redesign 1.52.14 giữ nguyên). RELEASE GATE: LOCAL ONLY — build-info + ?v= bump để npm run check PASS local, KHÔNG git push, KHÔNG deploy Vercel.",
  "build": "1.52.15"
}
