{
  "version": "1.52.14",
  "fingerprint": "1.52.14-knl-grade-proposal-create-form-redesign-LOCAL-ONLY",
  "builtAt": "2026-08-12T16:40:00+07:00",
  "notes": "1.52.14 [LOCAL ONLY, CHƯA PUSH/DEPLOY]: redesign form 'Tạo đề xuất' (Đề xuất nâng bậc) thành executive form — CHỈ tab này, 3 tab còn lại (Cần tôi xử lý/Đề xuất của tôi/Danh sách) và toàn bộ workflow Đồng ý/Duyệt KHÔNG đụng. TRACE trước khi code: subjectEmployeeCode trước là input tự do, chỉ validate lúc submit qua creationAuthorized() (self hoặc peopleScope match, lib/knl-grade-proposals.js) — cùng đúng scope mà listKnlPeople đã lọc, nên employee picker tái dùng NGUYÊN listKnlPeople, không backend mới. selectedFirstApproverEmployeeCode trước là input tự do 'Mã Trưởng ca xử lý', chỉ validate lúc submit qua resolveApprovalChain() nhánh Sales (approverQualifies: capabilities.agree_proposal===true + subjectMatchesScope(subject, grant.people_scope,...)) — KHÔNG có endpoint liệt kê trước, đây là gap thật nhưng fix tối thiểu (thuần READ, tái dùng ĐÚNG predicate, không business rule mới) nên KHÔNG STOP, đã triển khai theo đúng điều khoản 'chỉ đổi cách chọn, giữ nguyên payload/business rule'. Backend MỚI: getGradePromotionApproverOptions (lib/knl-grade-proposals.js) — liệt kê approver hợp lệ cho 1 subject bằng CHÍNH predicate đã dùng trong resolveApprovalChain(), gate requirePropose (cùng gate getGradeOptionsForSubject), wired vào server.js + api/data.js. getKnlGradeOptionsForSubject (current/next grade, tối đa 4 bậc) và createKnlGradePromotionProposal payload/validate KHÔNG đổi 1 dòng. Frontend: gpCreateFormHtml() viết lại hoàn toàn — 01 Nhân sự được đề xuất (searchable picker trên listKnlPeople đã lọc peopleScope, không free text, chọn xong hiện summary gọn Họ tên/Mã NV/Chức danh/Phòng ban/Chi nhánh), 02 Bậc hiện tại → Bậc đề xuất (card so sánh, chip chọn bậc dùng input radio ẩn + :has(input:checked) — cùng pattern .phfk-suit-options/.phfk-level-options đã có ở Survey, KHÔNG đổi value submit vẫn g.id), 03 Người nhận xử lý (CHỈ hiện khi backend báo required=true đúng điều kiện cũ, picker từ approverOptions.approvers — không free text, không thể gõ mã tuỳ ý, fail-closed hiện message rõ khi configured=false), 04 Lý do đề xuất (textarea + helper text hướng dẫn, giữ nguyên validate backend min 5 ký tự), Tóm tắt đề xuất (compact, chỉ render từ lựa chọn thật, không invent workflow status), CTA disabled đúng theo gpCreateCanSubmit(). Giữ nguyên nguyên tắc shared UI — cùng gpCreateFormHtml() cho mọi role, khác biệt chỉ ở peopleScope (employee picker) và approver eligibility (receiver picker) do backend trả, không fork form. Test: scripts/test-knl-grade-proposal-approver-options-2026-08.js (mới, 11 assertion backend: non-Sales/self-approver -> required=false, chưa cấu hình -> configured=false fail-closed, đúng danh sách approver theo scope + loại title-only/wrong-scope/inactive, permission gate, subject not found) — ALL PASS; extract pure-function test cho toàn bộ block render (18 assertion: picker suggestion/summary, grade chip giữ value cũ, approver block 4 trạng thái, canSubmit 5 case, summary block, escaping) — ALL PASS; regression toàn bộ test-knl-grade-proposals.js (workflow đầy đủ propose/agree/approve/reject/withdraw/broken-route/atomic) + people-income-gate + permissions-scope + income batch1 — ALL PASS, không có gì bị ảnh hưởng ngoài phạm vi. Không migration, không đổi peopleScope/incomeScope/permission model/approval status machine/Đồng ý/Duyệt workflow/Checklist/Classroom/Training Hub. RELEASE GATE: LOCAL ONLY — build-info bump để npm run check PASS local, KHÔNG git push, KHÔNG deploy Vercel.",
  "build": "1.52.14"
}
