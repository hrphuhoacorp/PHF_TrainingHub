{
  "version": "1.52.7",
  "fingerprint": "1.52.7-knl-next-grade-target",
  "builtAt": "2026-08-12T10:41:00+07:00",
  "notes": "1.52.7: 'Mục tiêu hướng tới bậc kế tiếp' — block mới ngay sau comparison matrix trên màn KNL cá nhân (assets/js/knl/phf-knl-app.js: buildCompetencyTargetDiff/competencyTargetHtml). Thuần client-side, KHÔNG API mới, KHÔNG migration — tái dùng foundationState.competency.currentStandard/nextStandard đã fetch sẵn từ getKnlEmployeeCompetencyStandard, join theo item.id thật (cùng pattern buildCompetencyMatrixGroups). Phân 4 nhánh trung tính: raise (next>current, 'Cần nâng chuẩn')/hold (next=current, 'Duy trì chuẩn', collapse qua <details>)/new (item chỉ có ở next, 'Yêu cầu mới')/changed (next<current hoặc bất thường, 'Chuẩn thay đổi', không diễn giải giảm chuẩn). Luôn dựa currentGrade/nextGrade THẬT của assignment, độc lập hoàn toàn với competencyWindowStart (nav browsing UI) — xác nhận qua test scenario 'browse B1↔B2 nhưng target vẫn B3→B4'. Max grade / no next-standard-groups có empty state riêng, không render diff rỗng. Wording tuyệt đối tránh 'bạn chưa đạt/còn thiếu/đạt X%' vì chưa có canonical assessment (chỉ so tiêu chuẩn văn bản). Phần thu nhập tham chiếu bậc kế tiếp KHÔNG implement trong batch này — trace xác nhận chưa tồn tại mapping competency_grade_id -> compensation_grade_id nào trong schema/code (2 bảng employee-assignment độc lập, chỉ chung employee_code text); STOP theo Technical Lead review, chờ PHF chốt mapping. Test: node scripts thủ công (extract pure functions buildCompetencyTargetDiff/competencyTargetHtml, 5 scenario: mix 4 nhánh, max grade, empty next standard, all-new, XSS-escaping) — ALL PASS, không có test file mới commit vào scripts/ (chỉ chạy tạm trong scratchpad, theo đúng pattern kiểm tra thủ công vì UI render logic không có test harness Node sẵn cho phf-knl-app.js). Bump cache-buster phf-knl.css/phf-knl-app.js trong index.html. Không sửa backend/lib, không đụng Salary/Income/Proposal business logic, không đổi peopleScope/incomeScope, không đổi navigation/matrix hiện có.",
  "build": "1.52.7"
}
